{
  "name": "backend",
  "version": "1.0.0",
  "description": "Create a server side to store user name and password",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "client": "npm run dev --prefix ../frontend",
    "dev": "concurrently \"nodemon server\" \"npm run client\""
  },
  "author": "Vinh",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^7.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.17.3",
    "pg": "^8.7.3"
  }
}
